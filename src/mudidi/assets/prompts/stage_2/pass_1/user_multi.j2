Study this dictionary and return a MDF parsing guide JSON object.

Inputs:
1. Introduction pages (attached below as images or PDF documents).
2. Several sample dictionary pages (transcriptions below; matching page images/PDFs follow in the same order).
{% if config_hint %}
{{ config_hint }}
{% endif %}

Return JSON with this shape:
{
  "markers": [
    {"marker": "lx", "description": "headword / lexeme (vernacular lemma)"},
    {"marker": "gn", "description": "Russian gloss / translation"}
  ],
  "rules": [
    "Separate homograph mains: two \\lx blocks with same lemma, each with \\hm N.",
    "If samples differ by section (e.g. main body vs appendix), state which rule applies where."
  ],
  "abbreviations": {"мн.": "plural"}
}

Include markers that appear on ANY sample page or are defined in the intro.
Omit markers not used. Keep descriptions short (one line each). Rules should cover
entry structure and gloss-line conventions across all samples. When samples show
different layouts, add section-specific rules rather than forcing one template.
Return JSON only — no markdown fences.

{{ sample_pages_block }}
